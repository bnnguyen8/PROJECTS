import { StyleSheet, Platform } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 20
    },
    title: {
        fontSize: 20,
        fontWeight: 'bold',
        paddingBottom: 10
    },
    optionContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 10,
    },
    optionText: {
        marginLeft: 10,
        fontSize: 17,
    },
})